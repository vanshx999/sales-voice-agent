# Core
openpyxl>=3.1.0
pandas>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# LLM
openai>=1.0.0

# Voice - TTS (macOS built-in, no extra deps needed)
# Voice - STT & Audio I/O (install for microphone input)
sounddevice>=0.4.0
numpy>=1.24.0
openai-whisper>=20230601

# TTS Engine
elevenlabs>=1.0.0      # ElevenLabs voice (free tier: 10K chars/month)

# Meeting Link (optional, for Google Calendar integration)
# google-api-python-client>=2.0.0
# google-auth-httplib2>=0.1.0
# google-auth-oauthlib>=1.0.0
