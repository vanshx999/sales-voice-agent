# Core
openpyxl>=3.1.0
pandas>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# LLM
openai>=1.0.0

# Voice - TTS (macOS built-in, no extra deps needed)
# Voice - STT & Audio I/O (install for microphone input)
sounddevice>=0.4.0
numpy>=1.24.0
openai-whisper>=20230601

# TTS Engine (choose one)
# elevenlabs>=1.0.0     # Best quality, free tier 10K chars/mo
# piper-tts>=1.0.0      # Local neural TTS (needs model download)

# Voice Pipeline (optional, for PSTN calling)
# livekit>=1.0.0         # LiveKit Agents
# twilio>=8.0.0          # Twilio for PSTN calls
